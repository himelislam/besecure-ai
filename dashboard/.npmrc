# @react-jvectormap/core@1.0.4's peerDependencies range hasn't been updated
# for React 19 yet, even though it works fine at runtime — without this,
# a fresh `npm install` (no existing node_modules/lockfile resolution to
# fall back on, e.g. on Vercel) fails with ERESOLVE.
legacy-peer-deps=true
